# CodeSim assets

Static assets for the desktop simulator UI (icons, themes, shell chrome).

The legacy Electron + React prototype lives under `simulator/legacy/` (archived).
CodeSim v0.1 uses the Rust `codesim-desktop` bootstrap; a native UI bridge is planned for v0.2.
