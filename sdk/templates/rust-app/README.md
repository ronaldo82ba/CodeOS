# Rust App Template

Starter template for CodeOS third-party applications.

```bash
codeos new myapp --template rust-app
cd myapp
codeos build
codeos run --simulator
```

Package format: `.capp` (zip archive with `codeos_manifest.toml`).
